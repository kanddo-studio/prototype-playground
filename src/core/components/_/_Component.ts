/**
 * Marker interface for ECS components.
 *
 * Keep this minimal so components remain plain data containers.
 * Systems and other framework code can narrow this interface when needed.
 */
export interface Component {}
